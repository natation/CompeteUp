(function(root) {
  'use strict';
  root.UserConstants = {
    CURRENT_USER_RECEIVED: "CURRENT_USER_RECEIVED",
    USERS_RECEIVED: "USERS_RECEIVED",
    ORGANIZER_RECEIVED: "ORGANIZER_RECEIVED"
  };
}(this));
