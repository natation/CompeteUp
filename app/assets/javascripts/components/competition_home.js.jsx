(function(root) {
  'use strict';
  var Link = ReactRouter.Link;
  root.CompetitionHome = React.createClass({
    render: function () {
      return (
        <RB.Grid>
          <h2>Events</h2>
        </RB.Grid>
      );
    }
  });
}(this));
          // <Link to={this.props}</Link>
