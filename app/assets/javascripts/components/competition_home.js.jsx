(function(root) {
  'use strict';
  var Link = ReactRouter.Link;
  root.CompetitionHome = React.createClass({
    render: function () {
      return (
        <RB.Grid>
          <h2>Upcoming Events (upcoming)...</h2>
        </RB.Grid>
      );
    }
  });
}(this));
